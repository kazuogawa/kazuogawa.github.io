interface:
  display_name: "Parallel Issue PRs"
  short_description: "未対応Issueをworktreeで並列実装しdraft PRを作成します"
  default_prompt: "$parallel-issue-prs を使い、PR未作成のopen Issueを最大3件、隔離worktreeで並列対応してください。"
policy:
  allow_implicit_invocation: false
